export type ObjectTag = number;

export const ObjectTagEnumMap: Record<string, number> =
{
    AttachedToWall: 0, // Include this tag if the object's back side must always be fully covered by voxelBlocks.
}
