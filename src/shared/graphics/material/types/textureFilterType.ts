// "nearest" for picture cells (no bleeding between cells); "linear" for lettering (smooth edges at
// any distance).
type TextureFilterType = "nearest" | "linear";

export default TextureFilterType;
