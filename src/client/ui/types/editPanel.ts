// A sub-panel raised from an object's edit options, named by the panel it shows (CompositionThumbnailPanel,
// CustomizeLabelTextPanel) so it can stay open across objects of different types.
export type EditPanel = "compositionThumbnail" | "labelText";
